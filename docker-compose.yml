version: '3.3'

services:
  bot:
    build: .
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./serif-list.csv:/serif-list.csv

  go:
    image: golang:1.22-alpine
    volumes:
      - .:/app
    working_dir: /app
